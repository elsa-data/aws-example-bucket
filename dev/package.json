{
  "name": "manual",
  "private": true,
  "version": "0.0.0",
  "description": "Manual CDK deployment for development",
  "scripts": {
    "deploy": "pnpm -w run build && cdk deploy",
    "destroy": "pnpm -w run build && cdk destroy"
  },
  "dependencies": {
    "aws-cdk": "2.99.1",
    "aws-cdk-lib": "2.99.1",
    "aws-data-buckets": "link:../packages/aws-data-buckets"
  },
  "devDependencies": {}
}
